spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springvalidation
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # shared across profiles
    show-sql: true
    properties:
      hibernate:
        format_sql: true